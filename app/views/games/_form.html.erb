<div class="container py-5">
  <div class="row">
    <div class="col-12 col-md-6 offset-lg-3">
      <div class="bg-white rounded p-3 shadow">
        <%= simple_form_for @game do |f| %>
        <h3><%= @text %></h3>
          <%= f.input :category, collection: Game::CATEGORIES %>
          <%= f.input :address, placeholder: '1 rue de Matignon' %>
          <%= f.input :description, placeholder: '' %>
          <%= f.input :photos, input_html: { multiple: true, id: 'photo-input', class: "d-none"}, label_html: { class: 'upload-photo'}, label: '📷 Upload the photos you want' %>
         <%# <%= image_tag "", width: 300, class: "hidden", id: "photo-preview" %>
          <%= f.input :price, placeholder: '€' %>
          <%= f.submit class: 'btn btn-flat' %>
        <% end %>
      </div>
    </div>
  </div>
</div>
